using System;
using System.Linq;
using System.Collections.Generic;

class Dictionary
{
    public static string BestScore(Dictionary<string, int> myDict)
    {
        foreach (KeyValuePair<string, int> entry in myDict)
        {
            if (bestScore.key == "None" || bestScore.Values < kv.value)
            {
                bestScore = kv;
            }
        }

        return bestScore.Key;
    }   
}